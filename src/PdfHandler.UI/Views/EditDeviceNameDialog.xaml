<Window x:Class="PdfHandler.UI.Views.EditDeviceNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="表示名を編集"
        Width="400"
        Height="150"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="デバイスの表示名:" FontWeight="SemiBold" Foreground="#333333"/>
        <TextBox x:Name="DisplayNameTextBox"
                 Grid.Row="2"
                 Padding="8"
                 FontSize="14"
                 Height="32"
                 VerticalContentAlignment="Center"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Height="32" Margin="0,0,10,0" IsDefault="True" Click="OkButton_Click"/>
            <Button Content="キャンセル" Width="80" Height="32" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
